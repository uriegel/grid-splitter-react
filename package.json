{
  "name": "grid-splitter-react",
  "version": "1.0.5",
  "description": "A grid splitter for React, resizable with splitter grid",
  "author": "uriegel",
  "license": "MIT",
  "repository": "uriegel/grid-splitter-react",
  "main": "dist/index.js",
  "module": "dist/index.modern.js",
  "source": "src/index.js",
  "engines": {
    "node": ">=10"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "microbundle-crl watch --no-compress --format modern,cjs",
    "build": "microbundle-crl --no-compress --format modern,cjs && copyfiles -f  src/*.d.ts dist"
  },
  "peerDependencies": {
    "react": "^17.0.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "cross-env": "^7.0.3",
    "microbundle-crl": "^0.13.11",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.2.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^4.0.2"
  }
}
